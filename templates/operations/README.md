# Operations template

A project / service review dashboard — throughput, service health, incidents.
Its job is to prove OpenIslands isn't trapped in personal life: the same islands
and compiler that render finance and health render an ops review with no
domain-specific hacks.

```bash
openislands validate .
openislands serve .
```
