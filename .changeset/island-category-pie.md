---
"@openislands/schema": minor
"@openislands/compiler": minor
"@openislands/runtime": minor
---

Add the `category.pie` island: a pie or donut chart of one series' share across a handful of categories. Slices sum by `label`, sort largest-first, and show their percentage; `donut: true` cuts an inner hole.
