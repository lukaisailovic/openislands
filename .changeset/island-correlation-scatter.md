---
"@openislands/schema": minor
"@openislands/compiler": minor
"@openislands/runtime": minor
---

Add the `correlation.scatter` island: a scatter or bubble plot of two numeric fields. Split points into colored series with `series`, scale them into bubbles with `size`, and label points for the tooltip; `xFormat`/`format` style the axes.
