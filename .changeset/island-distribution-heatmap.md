---
"@openislands/schema": minor
"@openislands/compiler": minor
"@openislands/runtime": minor
---

Add the `distribution.heatmap` island: a matrix heatmap of one value across two categorical dimensions (`x` × `y`), shaded on a continuous color scale with a legend.
