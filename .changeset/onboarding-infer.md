---
"openislands": minor
---

Add `openislands infer <file>` — infer a data file's schema, propose a canonical dataset contract with suggested islands, and `--bind` it into the manifest. `openislands add` now scaffolds every built-in island type.
