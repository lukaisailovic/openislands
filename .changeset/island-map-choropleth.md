---
"@openislands/schema": minor
"@openislands/compiler": minor
"@openislands/runtime": minor
---

Add the `map.choropleth` island: a geographic choropleth shading world-country regions by a value on a continuous scale. The world map ships as vendored GeoJSON so it renders fully offline (local-first — no tiles, no network); region names join on the GeoJSON country name.
