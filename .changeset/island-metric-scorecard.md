---
"@openislands/schema": minor
"@openislands/compiler": minor
"@openislands/runtime": minor
---

Add the `metric.scorecard` island: a compact card of several KPIs read off the last row, each with an optional delta versus the previous row. A tidy alternative to a row of separate `metric.kpi` tiles.
